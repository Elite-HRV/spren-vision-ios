import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
    poweredBy: {
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'center',
    },
    poweredByText: {
        fontSize: 12,
        fontWeight: '700',
        marginRight: 5,
    },
});
